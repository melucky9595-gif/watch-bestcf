name: Watch File
on:
  schedule:
    - cron: '0 0,12 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download target file
        run: |
          mkdir -p current
          curl -sL -o current/ctcc-ip.txt "https://github.com/DustinWin/bestcf/releases/download/bestcf/ctcc-ip.txt"

      - name: Compare hash
        id: diff
        run: |
          mkdir -p hashes
          NEW_HASH=$(sha256sum current/ctcc-ip.txt | awk '{print $1}')
          OLD_HASH=$(cat hashes/ctcc-ip.txt.hash 2>/dev/null || echo "none")
          if [ "$NEW_HASH" != "$OLD_HASH" ]; then
            echo "changed=true" >> $GITHUB_OUTPUT
            echo "$NEW_HASH" > hashes/ctcc-ip.txt.hash
          else
            echo "changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Notify Telegram
        if: steps.diff.outputs.changed == 'true'
        run: |
          curl -s -X POST https://api.telegram.org/bot${{ secrets.TG_BOT_TOKEN }}/sendMessage \
            -d chat_id=${{ secrets.TG_CHAT_ID }} \
            -d text="ctcc-ip.txt 文件已更新"

      - name: Speed test IPs
        run: |
          set +e
          > result.txt
          while IFS= read -r line; do
            [ -z "$line" ] && continue
            ip="${line%%#*}"
            clean_ip=$(echo "$ip" | tr -d '[]')
            ms=$(curl -s -o /dev/null --max-time 3 \
              --resolve speed.cloudflare.com:443:"$clean_ip" \
              -w "%{time_connect}" \
              "https://speed.cloudflare.com/__down?bytes=1000" 2>/dev/null)
            if [ -n "$ms" ] && [ "$ms" != "0.000000" ]; then
              ms_int=$(awk "BEGIN{printf \"%d\", $ms*1000}")
              echo "$ms_int $ip" >> result.txt
            fi
          done < current/ctcc-ip.txt
          sort -n result.txt | head -5 | awk '{print $2"#"$1"ms"}' > docs_data.txt
          cat docs_data.txt

      - name: Build outputs
        run: |
          mkdir -p docs
          touch docs/.nojekyll
          cp docs_data.txt docs/data.txt

      - name: Commit changes
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add hashes/ docs/
          git diff --cached --quiet || git commit -m "update data"
          git push
